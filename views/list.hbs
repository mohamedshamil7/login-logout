<section>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                {{#each products}}
                <ul class="list-group">
  <li class="list-group-item disabled" >
    <img style="width: 75px;" src="{{this.Image}}" alt="">
    <h4>{{this.id}}.{{this.name}}</h4>
  <p>{{this.Category}}</p><br>
  <p>{{this.description}}</p></li>
  
</ul>
{{/each}}
            </div>
        </div>
    </div>
</section>