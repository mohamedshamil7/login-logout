<section>
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                {{#each products}}
                <ul class="list-group">
  <li class="list-group-item disabled" >
    <img style="width:200px ;" src="{{this.imageUrl}}" alt="">
    <h4>{{this.name}}</h4>
    <h4>{{this.brand}}</h4>
  <p>{{this.price}}</p><br>
<a href="#" class="btn btn-primary">Buy</a>
  </li>
  
</ul>
{{/each}}
            </div>
        </div>
    </div>
</section>